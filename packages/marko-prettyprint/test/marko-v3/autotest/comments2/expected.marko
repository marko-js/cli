<!-- Colors available -->
<if(notEmpty(colors))>
    <ul>
        <for(color in colors)>
            <li>$color</li>
        </for>
    </ul>
</if>

~~~~~~~
// Colors available
if(notEmpty(colors))
    ul
        for(color in colors)
            li -- $color
