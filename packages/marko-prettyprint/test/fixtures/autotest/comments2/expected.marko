<!-- Colors available -->
<if test="notEmpty(colors)">
    <ul>
        <for each="color in colors">
            <li>
                $color
            </li>
        </for>
    </ul>
</if>

~~~~~~~
// Colors available
if test="notEmpty(colors)"
    ul
        for each="color in colors"
            li
                - $color
