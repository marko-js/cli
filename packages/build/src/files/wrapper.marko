$ const _flush = out.flush;
$ const _end = out.end;
$ out.flush = function () {
    input.renderAssets(this, true);
    _flush.apply(this, arguments);
}
$ out.end = function () {
    input.renderAssets(this);
    _end.apply(this, arguments);
}

<${input.template}/>
<init-components/>